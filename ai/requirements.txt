# AutoBrain AI gateway + market-data scraper (AUT-1242-C3 merged image)
fastapi==0.133.0
starlette==1.3.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-multipart==0.0.32
structlog==24.4.0
slowapi==0.1.9
playwright==1.62.0
playwright-stealth==2.0.3

# Local OCR fallback (Tesseract via pytesseract, optional runtime dep)
pytesseract==0.3.13
pillow==12.3.0
pypdf==6.16.1

# AUT-1189: pin transitives flagged by osv-scanner so the PR-time pip-audit
# gate (security-pr-gate.yml, `--no-deps` mode) catches a known-vulnerable
# resolved version — the gate audits direct pins only, so an unpinned
# transitive inherits any build-time resolution. httpx pulls idna; jinja2
# (transitive via fastapi templates / reportlab fallback) pulls pygments.
idna==3.18
pygments==2.20

# Test/dev
pytest==9.1.1
pytest-asyncio==1.3.0
ruff==0.8.6
