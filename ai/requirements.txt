# AutoBrain AI gateway + market-data scraper (AUT-1242-C3 merged image)
fastapi==0.133.0
starlette==1.3.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-multipart==0.0.32
structlog==24.4.0
slowapi==0.1.9
playwright==1.62.0
playwright-stealth==2.0.3

# Local OCR fallback (Tesseract via pytesseract, optional runtime dep)
pytesseract==0.3.13
pillow==12.3.0
pypdf==6.16.1

# AUT-1189: transitive floor pins for known-CVE packages. Without these,
# image builds can resolve to older vulnerable versions if an upstream
# constraint drifts. Mirrors the pins in backend/requirements.txt.
idna>=3.15            # PYSEC-2026-215 / GHSA-65pc-fj4g-8rjx (CVE 3.x<3.15)
pycryptodome>=3.19.1  # PYSEC-2026-1811 / GHSA-j225-cvw7-qrx7 (CVE 3.x<3.19.1)
pygments>=2.20        # PYSEC-2023-117 + PYSEC-2026-2987 (CVEs 2.x<2.20)

# Test/dev
pytest==9.1.1
pytest-asyncio==1.3.0
ruff==0.8.6
