# AutoBrain AI gateway + market-data scraper (AUT-1242-C3 merged image)
fastapi==0.133.0
starlette==1.3.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-multipart==0.0.32
structlog==24.4.0
slowapi==0.1.9
playwright==1.62.0
playwright-stealth==2.0.3

# Local OCR fallback (Tesseract via pytesseract, optional runtime dep)
pytesseract==0.3.13
pillow==12.3.0
pypdf==6.16.1

# AUT-1189: exact transitive pins for known-CVE packages. The resolved-tree
# pip-audit pass (security-pr-gate.yml) audits the *resolved* environment, so
# these must be exact `==` pins (a `>=` floor lets pip resolve to the latest,
# which the gate cannot pin and which can drift past the fix). httpx pulls
# idna; jinja2 (transitive via fastapi templates / reportlab fallback) pulls
# pygments. Mirrors the pins in backend/requirements.txt.
idna==3.18            # PYSEC-2026-215 / GHSA-65pc-fj4g-8rjx (CVE 3.x<3.15)
pygments==2.20        # PYSEC-2023-117 + PYSEC-2026-2987 (CVEs 2.x<2.20)

# Test/dev
pytest==9.1.1
pytest-asyncio==1.3.0
ruff==0.8.6
