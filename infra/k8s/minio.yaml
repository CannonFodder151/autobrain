apiVersion: apps/v1
kind: Deployment
metadata:
  name: autobrain-minio
  namespace: autobrain
spec:
  replicas: 1
  selector:
    matchLabels:
      app: autobrain
      component: minio
  template:
    metadata:
      labels:
        app: autobrain
        component: minio
    spec:
      securityContext:
        # ponytail: no runAsNonRoot here — minio entrypoint writes /opt/bin at
        # start; revisit when image supports a clean read-only non-root run.
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: minio
          # AB-INFRA-009: pinned to the same immutable release as compose files.
          image: minio/minio:RELEASE.2025-09-07T16-13-09Z
          args: ["server", "/data", "--console-address", ":9001"]
          ports:
            - containerPort: 9000
            - containerPort: 9001
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: autobrain-secrets
                  key: minio-access-key
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: autobrain-secrets
                  key: minio-secret-key
          readinessProbe:
            httpGet:
              path: /minio/health/live
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /minio/health/live
              port: 9000
            initialDelaySeconds: 30
            periodSeconds: 15
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: autobrain-minio-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: autobrain-minio-pvc
  namespace: autobrain
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: autobrain-minio
  namespace: autobrain
spec:
  selector:
    app: autobrain
    component: minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
