fastapi==0.133.0
starlette==1.3.1
uvicorn==0.34.0
slowapi==0.1.9
pydantic==2.13.4
httpx==0.28.1
playwright==1.62.0
playwright-stealth==2.0.3

# AUT-1189: exact transitive pins for known-CVE packages. The resolved-tree
# pip-audit pass (security-pr-gate.yml) audits the *resolved* environment, so
# these must be exact `==` pins (a `>=` floor lets pip resolve to the latest,
# which the gate cannot pin and which can drift past the fix). httpx pulls
# idna; PyJWT[crypto] pulls pycryptodome; jinja2 (transitive via fastapi
# templates / reportlab fallback) pulls pygments. Mirrors the pins in backend/requirements.txt.
idna==3.18            # PYSEC-2026-215 / GHSA-65pc-fj4g-8rjx (CVE 3.x<3.15)
pycryptodome==3.23    # PYSEC-2026-1811 / GHSA-j225-cvw7-qrx7 (CVE 3.x<3.19.1)
pygments==2.20        # PYSEC-2023-117 + PYSEC-2026-2987 (CVEs 2.x<2.20)
