"""Vehicle domain module.

Owns vehicle CRUD, ownership rules and the unified timeline. Route:
`app.api.v1.vehicles`; service: `app.services.vehicle`; models:
`app.models.vehicle`; schemas: `app.schemas.vehicle`.
"""
