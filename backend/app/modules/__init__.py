"""Domain module packages.

Each sub-package owns its models, schemas, services and routes. The flat
`app.models`, `app.schemas`, `app.services` and `app.api.v1` layers remain as
compatibility shims during the incremental migration (AUT-3814).
"""
