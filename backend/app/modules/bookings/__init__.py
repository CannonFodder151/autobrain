"""Booking domain module.

Owns engineer booking requests (create, list, respond, cancel). Routes:
`app.api.v1.booking_requests`; service: `app.services.engineer_dashboard`;
models: `app.models.engineer`; schemas: `app.schemas.engineer_dashboard`.
"""
