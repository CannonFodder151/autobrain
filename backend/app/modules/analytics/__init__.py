"""Analytics domain module.

Owns spend summaries, cost forecasts and per-vehicle analytics. Route:
`app.api.v1.analytics`; service: `app.services.ownership` (analytics helpers);
schemas: `app.schemas.analytics`.
"""
