# Website Documentation (autobrainservice.app)

## Overview

The marketing website lives at **autobrainservice.app** and is a separate repository: **CannonFodder151/autobrainservice-website** (private). It is a static site (vanilla HTML/CSS/JS) deployed to the Oracle VM (152.69.188.133) via NGINX (Portainer endpoint 3). The site does **not** use a build step or framework — every page is a `.html` file.

**Domain:** `autobrainservice.app` (Cloudflare DNS, managed by Deployment team).  
**Subdomains:**
- `demo.autobrainservice.app` — demo instance
- `hub.autobrainservice.app` — federation hub (when live)

## Repository Structure

```
autobrainservice-website/
├── index.html              # Homepage
├── features.html           # Feature overview
├── hosted.html             # Hosted (AutoBrain-Hosted) offering
├── selfhost.html           # Self-hosting guide
├── ai-data.html            # AI data & privacy page
├── blog.html               # Blog index
├── blog/*.html             # Individual posts (33 posts)
├── community-garage.html   # Community Garage teaser (coming soon)
├── about.html              # About / team
├── contact.html            # Contact / lead capture
├── privacy.html            # Privacy policy
├── changelog.html          # Customer-facing changelog
├── assets/                 # Images, OG images, favicon
│   ├── og-image.png        # Shared fallback OG image (1200×400)
│   └── blog/og/<slug>.png  # Per-post OG images (1200×630, 33 files, AUT-3115)
├── nav.js                  # Shared navigation
├── lead.js                 # Lead capture → n8n webhook
└── *.html                  # Other pages (rego, diagnostics, fuel, etc.)
```

## Page Inventory & Purpose

| Page | URL | Purpose | Key CTAs |
|------|-----|---------|----------|
| index.html | `/` | Hero, value props, social proof, footer | "Try the demo", "Self-host", "View features" |
| features.html | `/features.html` | Module breakdown (Garage, Fuel, Rego, Diagnostics, AI, Social) | Demo / Self-host links per module |
| hosted.html | `/hosted.html` | AutoBrain-Hosted offering (free forever, managed) | "Start free" → demo or signup |
| selfhost.html | `/selfhost.html` | Self-host guide (Docker, requirements, FAQ) | GitHub repo, Docker Hub, Discord |
| ai-data.html | `/ai-data.html` | AI architecture, deterministic-first, data ownership | Demo link, self-host link |
| blog.html | `/blog.html` | Blog index with section filters | Read post, subscribe |
| community-garage.html | `/community-garage.html` | Coming-soon teaser, waitlist capture | "Join waitlist" → n8n |
| about.html | `/about.html` | Team, mission, open-source values | GitHub, Discord |
| contact.html | `/contact.html` | Lead form → n8n webhook → Discord #support | Submit |
| changelog.html | `/changelog.html` | Customer-facing release history | — |
| [topic pages] | `/*.html` | SEO landing pages (rego, fuel, OBD, clubs, etc.) | Demo / Self-host |

## Deployment

- **Target:** Oracle VM (152.69.188.133) — Portainer endpoint 3 (NGINX-Host)
- **Method:** Static files served by NGINX container. No build pipeline.
- **SSL:** Cloudflare Full (strict) + Let's Encrypt on NGINX.
- **Cache headers:** `Cache-Control: public, max-age=31536000, immutable` for `/assets/*`; `no-cache` for HTML.
- **Deploy process:** Push to `main` → Deployment team runs `docker compose pull && docker compose up -d` on NGINX-Host, or uses Portainer "Redeploy" on the stack. No CI/CD pipeline for this repo.

## SEO & Social Metadata

- **Canonical:** `https://autobrainservice.app/` on all pages.
- **OG/Twitter cards:** Every page has `og:title`, `og:description`, `og:image`, `og:url`, `twitter:card`, `twitter:image`.
- **Per-post OG images:** 33 blog posts have unique `og:image` at `/assets/blog/og/<slug>.png` (1200×630), generated by AUT-3115. Fallback is `/assets/og-image.png` (1200×400).
- **JSON-LD:** Organization, WebSite, SoftwareApplication, BlogPosting schemas on relevant pages.
- **Author schema:** Blog posts use `@type: Person` (Nathan) per AUT-3115; site-wide fallback is Organization.

## Content Ownership & Workflow

- **Source of truth:** `autobrainservice-website` repo (`main` branch).
- **Marketing copy changes:** CMO agent drafts → human CMO approves in Discord `#marketing` via n8n Reporter embed → CMO agent commits to repo → Deployment team deploys.
- **Phase 1 marketing refresh branch:** `phase1/cmo-marketing-refresh` (tracks updated copy for all pages).
- **Blog posts:** Drafted in Outline → approved in Discord `#marketing` → committed as `.html` in `blog/` + per-post OG image in `assets/blog/og/`.
- **Lead capture:** `lead.js` posts to n8n webhook (`https://n8n.nathanmartina.com/webhook/lead`) → creates Discord thread in `#support` + logs to Outline.

## Analytics & Tracking

- **No Google Analytics / third-party trackers.** Privacy-first.
- **Server-side logs:** NGINX access logs only (IP anonymised).
- **Custom events:** Lead form submits, demo clicks, GitHub clicks tracked via n8n webhook to Outline.

## Related Docs

- [Demo Environment](./demo-environment.md) — demo.autobrainservice.app ops
- [Social Media Strategy](./social.md) — social channels, Buffer workflow
- [Content Calendar](./content-calendar.md) — scheduled campaigns
- [Growth Metrics](./growth-metrics.md) — KPIs, funnel, Buffer analytics
- [Per-Post OG Image Workflow](./per-post-og-image-workflow.md) — AUT-3115 image generation

Source: `autobrainservice-website` repo + Phase 1 marketing refresh branch `phase1/cmo-marketing-refresh`.