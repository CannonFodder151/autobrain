# Drop master screenshot artwork here (PNG, recommended 1242x2688 or larger).
# The `fastlane ios screenshots` lane reframes and uploads per device class.
# Filenames should encode the order: `01-dashboard.png`, `02-trips.png`, etc.
